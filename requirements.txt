# Complete pinned environment. Install from this file — there is no separate lock.
# Verified on CPython 3.10.11, Windows x86-64, CPU only.
# JAX is held at the version the upstream authors tested; do not upgrade it
# without rerunning the experiments, because sampler and PRNG behaviour matter.

# --- Direct dependencies -----------------------------------------------------
jax==0.4.26
jaxlib==0.4.26
equinox==0.11.4
optax==0.2.2
chex==0.1.86
numpy==1.26.4
scipy==1.12.0
h5py==3.11.0
nptyping==2.5.0
jaxtyping==0.2.28
ml-dtypes==0.3.2
matplotlib==3.8.4            # analysis figures, and the authors' visualize_runs.py
tqdm==4.66.4                 # progress bars in the authors' training loop

# --- Pulled in by the above; pinned so the environment is reproducible -------
absl-py==2.5.0
colorama==0.4.6
contourpy==1.3.2
cycler==0.12.1
fonttools==4.65.0
kiwisolver==1.5.1
opt_einsum==3.4.0
packaging==26.3
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
toolz==1.1.0
typeguard==2.13.3
typing_extensions==4.16.0
